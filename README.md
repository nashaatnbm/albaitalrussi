# 🐻 البيت الروسي — Al-Bait Al-Russi

متجر إلكتروني للمنتجات الروسية الأصيلة مع لوحة تحكم، نظام حسابات، وإشعارات تيليجرام.

---

## 🚀 تشغيل المشروع

### 1. المتطلبات
- Node.js (v18+)
- MongoDB (محلي أو Atlas)

### 2. تثبيت المكتبات
```bash
npm install
```

### 3. إعداد المتغيرات
انسخ ملف `.env.example` لـ `.env` وعدّل القيم:
```bash
cp .env.example .env
```

ثم افتح `.env` وأضف:
```
PORT=3000
MONGODB_URI=mongodb://localhost:27017/albait-el-rossi
SESSION_SECRET=اختر_كلمة_سرية_قوية_هنا
BOT_TOKEN=توكن_البوت_من_تيليجرام
ADMIN_CHAT_ID=معرف_محادثة_التيليجرام
ADMIN_EMAIL=your_admin@email.com
```

### 4. تشغيل المشروع
```bash
# للتطوير (مع إعادة تشغيل تلقائية)
npm run dev

# للإنتاج
npm start
```

افتح: **http://localhost:3000**

---

## 📋 الصفحات

| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| الرئيسية | `/` | صفحة المنتجات |
| تسجيل الدخول | `/auth/login` | تسجيل دخول / إنشاء حساب |
| الطلب | `/checkout` | اختيار المنتجات وبيانات الشحن |
| تأكيد الطلب | `/orders/:id/success` | صفحة نجاح الطلب |
| طلباتي | `/my-orders` | طلبات العميل السابقة |
| الأدمن | `/admin` | لوحة التحكم |
| العملاء | `/admin/customers` | إدارة العملاء |

---

## 🤖 إعداد بوت تيليجرام

### الخطوة 1: إنشاء البوت
1. افتح تيليجرام وابحث عن `@BotFather`
2. أرسل `/newbot`
3. اختر اسماً للبوت
4. ستحصل على **BOT_TOKEN** — انسخه في `.env`

### الخطوة 2: الحصول على Chat ID
1. افتح المحادثة مع بوتك
2. أرسل أي رسالة
3. افتح في المتصفح:
   ```
   https://api.telegram.org/bot<BOT_TOKEN>/getUpdates
   ```
4. ابحث عن `"chat":{"id":XXXXXXX}` — هذا هو **ADMIN_CHAT_ID**

---

## 👤 إعداد حساب الأدمن

**الطريقة 1:** أول مستخدم يسجّل يصبح أدمن تلقائياً.

**الطريقة 2:** ضع إيميلك في `.env`:
```
ADMIN_EMAIL=your@email.com
```
أي حساب بهذا الإيميل سيكون أدمن.

---

## 📁 هيكل المشروع

```
albait-el-rossi/
├── models/
│   ├── User.js          # نموذج المستخدم
│   └── Order.js         # نموذج الطلب
├── routes/
│   ├── auth.js          # مسارات تسجيل الدخول
│   ├── admin.js         # مسارات الأدمن
│   └── checkout.js      # مسارات الطلبات
├── controllers/
│   ├── authController.js
│   ├── adminController.js
│   └── checkoutController.js
├── middleware/
│   └── auth.js          # حماية المسارات
├── utils/
│   └── telegram.js      # إشعارات تيليجرام
├── views/
│   ├── partials/        # header + footer
│   ├── auth/            # صفحة تسجيل الدخول
│   ├── admin/           # لوحة التحكم
│   └── checkout/        # الطلب والتأكيد
├── public/
│   ├── css/style.css
│   └── js/main.js
├── app.js               # نقطة البداية
├── .env.example
└── README.md
```
